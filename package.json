{
  "name": "estimatex",
  "private": true,
  "version": "0.1.0",
  "license": "UNLICENSED",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter @estimatex/web dev",
    "dev:api": "pnpm --filter @estimatex/api start:dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @estimatex/web start"
  }
}

